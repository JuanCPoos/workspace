/**
 * OBJETOS
 * Un objeto es una agrupación de datos que tienen sentido entre si
 */

const miPrimerObjeto = {} //objeto vació 

/*
*los objetos no tienen variables sino propiedades para asignarle. Definimos entre llaves las propiedas
*que va a tener el objeto. los valores asignados se llamos "valores primitivos" (en este caso)
*/
const miObjeto = {  
    unNumero: 12,
    unString: 'Esta cadena de caracteres',
    unaCondicion: true,
}

console.log('Objeto', miObjeto.unaCondicion)

